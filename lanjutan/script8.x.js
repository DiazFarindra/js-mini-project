// Spread Operator

const data = ['farindra', 'diaz', 'ibrahim']
const data1 = ['farindra', 'diaz', 'ibrahim']
console.log([...data, ...data1])

// const flash_data = document.querySelectorAll('.flash-data')
// const data = [...flash_data].map((flash_data) => flash_data.dataset.flashdata)
// console.log(data)
